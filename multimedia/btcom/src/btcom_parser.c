#include "../../../ax32_platform_demo/application.h"
#include "../inc/btcom.h"
#include "../inc/btcom_inner.h"


#if 0
// 全局数据
static CMD_PARSER_T   sCmdParser[] = {
	{BTCOM_GET_STATE,         NULL}, // = 0xA3,
	{BTCOM_SET_QUALITY,       NULL}, // = 0xA4,
	{BTCOM_SEND_BITMAP,       NULL}, // = 0xA5,
	{BTCOM_FEED,              NULL}, // = 0xA1,
	{BTCOM_SEND_LINE_DATA,    NULL}, // = 0xA2,
	{BTCOM_GET_DEVINFO,       NULL}, // = 0xA8,
	{BTCOM_FLOW_CONTROL,      NULL}, // = 0xAE,
	{BTCOM_SET_ENERGE25,      NULL}, // = 0xAF,
	{BTCOM_SEND_LINE_DATA_ZIP,NULL}, // = 0xBF,
	{BTCOM_SET_PRINT_SPEED,   NULL}, // = 0xBE,
	{BTCOM_SET_MOTOR_STEP_TIME,NULL},// = 0xBD,
	{BTCOM_PRINT_IGNORE,      NULL}, // = 0xBC,
	{BTCOM_SET_LED_SCREEN,    NULL}, // = 0xA6,
	{BTCOM_SET_FLAG_BIT,      NULL}, // = 0xBC,
	{BTCOM_WRITE_DEVICE_ID,   NULL}, // = 0xBB,
	{BTCOM_SET_DENSITY,       NULL}, // = 0xF2,
};

/**
 * 解析器初始化
 *
 */
void btcomParserInit(void)
{
	int i, j;
	u8 min_cmd;

	for (i = 0; i < sizeof(sCmdParser)/sizeof(sCmdParser[0]) - 1; i++) {
		min_cmd = i;
		for (j = i + 1; j < sizeof(sCmdParser)/sizeof(sCmdParser[0]); j++) {
			if (sCmdParser[j].cmd < sCmdParser[min_cmd].cmd) {
				min_cmd = j;
			}
		}
		if (min_cmd != i) {
			CMD_PARSER_T tmp;
			tmp.cmd = sCmdParser[min_cmd].cmd;
			tmp.fnParser = sCmdParser[min_cmd].fnParser;
			sCmdParser[min_cmd].cmd = sCmdParser[i].cmd;
			sCmdParser[min_cmd].fnParser = sCmdParser[i].fnParser;
			sCmdParser[i].cmd = tmp.cmd;
			sCmdParser[i].fnParser = tmp.fnParser;
		}
	}
}

/**
 * 判断是否支持给定命令
 *
 * cmd - 给定命令
 *
 * 返回:true - 支持；false - 不支持
 *
 */
bool btcomCmdSupport(u8 cmd)
{
	int begin, end, mid;
	bool ret = false;

	begin = 0;
	end = sizeof(sCmdParser)/sizeof(sCmdParser[0]) - 1;
	while (begin <= end) {
		mid = (begin + end)>>1;
		if (sCmdParser[mid].cmd == cmd) {
			ret = true;
			break;
		} else if (sCmdParser[mid].cmd < cmd) {
			begin = mid + 1;
		} else {
			end = mid - 1;
		}
	}

	return ret;
}
#else
// 全局数据
static u8   sCmdParser[] = {
	BTCOM_SET_BTNAME,		   // = 0x02,
	BTCOM_GET_STATE,           // = 0xA3,
	BTCOM_SET_QUALITY,         // = 0xA4,
	BTCOM_SEND_BITMAP,         // = 0xA5,
	BTCOM_FEED,                // = 0xA1,
	BTCOM_SEND_LINE_DATA,      // = 0xA2,
	BTCOM_GET_DEVINFO,         // = 0xA8,
	BTCOM_FLOW_CONTROL,        // = 0xAE,
	BTCOM_SET_ENERGE25,        // = 0xAF,
	BTCOM_SEND_LINE_DATA_ZIP,  // = 0xBF,
	BTCOM_SET_PRINT_SPEED,     // = 0xBE,
	BTCOM_SET_MOTOR_STEP_TIME, // = 0xBD,
	//BTCOM_PRINT_IGNORE,        // = 0xBC,
	BTCOM_SET_LED_SCREEN,      // = 0xA6,
	BTCOM_SET_FLAG_BIT,        // = 0xBC,
	BTCOM_WRITE_DEVICE_ID,     // = 0xBB,
	BTCOM_SET_DENSITY,         // = 0xF2,
};

/**
 * 解析器初始化
 *
 */
void btcomParserInit(void)
{
	int i, j;
	u8 min_cmd;

	for (i = 0; i < sizeof(sCmdParser)/sizeof(sCmdParser[0]) - 1; i++) {
		min_cmd = i;
		for (j = i + 1; j < sizeof(sCmdParser)/sizeof(sCmdParser[0]); j++) {
			if (sCmdParser[j] < sCmdParser[min_cmd]) {
				min_cmd = j;
			}
		}
		if (min_cmd != i) {
			u8 tmp;
			tmp = sCmdParser[min_cmd];
			sCmdParser[min_cmd] = sCmdParser[i];
			sCmdParser[i] = tmp;
		}
	}
}

/**
 * 判断是否支持给定命令
 *
 * cmd - 给定命令
 *
 * 返回:true - 支持；false - 不支持
 *
 */
bool btcomCmdSupport(u8 cmd)
{
	int begin, end, mid;
	bool ret = false;

	begin = 0;
	end = sizeof(sCmdParser)/sizeof(sCmdParser[0]) - 1;
	while (begin <= end) {
		mid = (begin + end)>>1;
		if (sCmdParser[mid] == cmd) {
			ret = true;
			break;
		} else if (sCmdParser[mid] < cmd) {
			begin = mid + 1;
		} else {
			end = mid - 1;
		}
	}

	return ret;
}
#endif

/**
 * 计算checksum
 *
 * data - 要校验的数据
 * len - 数据长度
 * init_crc - crc初始值
 *
 * 返回:checksum
 *
 */
static const u8 CRC8_TAB[]={
    ,
};
u8 btcomCheckSum(const u8 *data,int len,u8 init_crc)
{
	u8 crc=init_crc;
	int i;

	for(i=0;i<len;i++)
	{
		crc = CRC8_TAB[crc^data[i]];
	}
	return crc;
}

